<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Deposit History</title>
    <%- include('partials/head-links') %>
</head>
<body>
    <%- include('partials/usernav') %>
    <div class="aboutHead">
        <h3 class="about-us-heading text-center my-4">Deposit History</h3>
    </div>

    <div class="container-fluid">
        <div class="row mt-sm-5 mx-sm-5 m-2 ">
            <div class="col-3 border border-white p-3"><b>ID</b></div>
            <div class="col-3 border border-white p-3"><b>AMOUNT</b></div>
            <div class="col-3 border border-white p-3"><b>STATUS</b></div>
            <div class="col-3 border border-white p-3"><b>DATE</b></div>
        </div>
    </div>
    
    <% if (deposits) { %>
        <div class="container-fluid">
            <% deposits.forEach(deposit =>{ %>
                <div class="row mx-sm-5 mx-2 ">
                    <div class="col-3 p-3 border border-white text-nowrap overflow-auto"><b><%= deposit.transactID %> </b></div>
                    <div class="col-3 p-3 border border-white text-nowrap overflow-auto "><b>$<%= deposit.amount %> </b></div>
                    <div class="col-3 p-3 border border-white text-nowrap"><b><%= deposit.status %> </b></div>
                    <div class="col-3 p-3 border border-white check text-nowrap overflow-auto"><b><%= deposit.date %> </b></div>
                </div>
            <% }) %> 
        </div>
    <% } %>


    
</body>
</html>